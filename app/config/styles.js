import colors from './colors';
import { ImageBackground } from 'react-native';

export default {
	colors,
	text: {
		fontSize: 18,
		textAlign: 'right',
		color: colors.dark,
		width: '100%',
		fontFamily: 'app_l',
	},
};
